spring:
  application:
    name: candy

  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

    hikari:
      connection-test-query: SELECT 1
      validation-timeout: 3000
      auto-commit: false
      data-source-properties:
        prepareThreshold: 0       # 🔥 PostgreSQL 서버측 prepared statement 비활성화
        preparedStatementCacheQueries: 0

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc.statement_cache.size: 0
        statement_cache.size: 0
    show-sql: true

  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: Ap6rPG4ps7AgPG4l_DPi
            client-secret: 4LXJQBePIG
            redirect-uri: "{baseUrl}/login/oauth2/code/naver"
            authorization-grant-type: authorization_code
            scope:
              - name
              - email
            client-name: Naver

          kakao:
            client-id: a9f37adeecdea3bdc79ed7474c789862
            client-secret:
            redirect-uri: https://selfmade-umqd.onrender.com/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            client-authentication-method: none
            scope:
              - profile_nickname
            client-name: Kakao

        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

file:
  upload-dir: C:/uploads/images

kakao:
  pay:
    host: https://kapi.kakao.com
    admin-key: e41d05f8c5e81d201b0ba0c70ed18c25 # 실제 발급받은 관리자 키
    cid: TC0ONETIME          # 가맹점 코드 (테스트용), 코드작성 X
    ready-path: /payment/ready  #카카오페이 호출 주소, 코드작성 X
    approve-path: /payment/approve  #카카오페이 인증 주소, 코드작성 X

naver:
  pay:
    host: https://sandbox-nsp.pay.naver.com
    merchant-id: np_odjni544553
    client-id: HN3GGCMDdTgGUfl0kFCo
    client-secret: ftZjkkRNMR
    chain-id: MG5ZbE9ZaXRrZE4

jwt:
  secret: ${JWT_SECRET:MySuperSecretkeyForJwtGeneration123456}

openai:
  api-key: ${OPENAI_API_KEY}
