spring:
  application:
    name: candy

  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

    hikari:
      connection-test-query: SELECT 1
      validation-timeout: 3000
      auto-commit: false
      data-source-properties:
        prepareThreshold: 0       # 🔥 PostgreSQL 서버측 prepared statement 비활성화
        preparedStatementCacheQueries: 0

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc.statement_cache.size: 0
        statement_cache.size: 0
    show-sql: true

  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: Ap6rPG4ps7AgPG4l_DPi
            client-secret: 4LXJQBePIG
            redirect-uri: "{baseUrl}/login/oauth2/code/naver"
            authorization-grant-type: authorization_code
            scope:
              - name
              - email
            client-name: Naver

          kakao:
            client-id: 0c7f1ee8455f9a00270728ad7ad196ec
            client-secret:
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            client-authentication-method: none
            scope:
              - profile_nickname
            client-name: Kakao

        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

file:
  upload-dir: C:/uploads/images

kakao:
  pay:
    host: https://kapi.kakao.com
    admin-key: ed1df4b9d47e4cb603d7dbc8e7124cbb
    cid: TC0ONETIME
    ready-path: /payment/ready
    approve-path: /payment/approve

naver:
  pay:
    host: https://sandbox-nsp.pay.naver.com
    merchant-id: np_odjni544553
    client-id: HN3GGCMDdTgGUfl0kFCo
    client-secret: ftZjkkRNMR
    chain-id: MG5ZbE9ZaXRrZE4

jwt:
  secret: ${JWT_SECRET:MySuperSecretkeyForJwtGeneration123456}

openai:
  api-key: ${OPENAI_API_KEY}
